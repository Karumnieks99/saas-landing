Labelled text input with a zinc border and an orange brand focus ring.

```jsx
<Input label="Work email" type="email" placeholder="you@company.com" hint="No credit card required" />
```

Accepts all native `<input>` props (type, placeholder, value, onChange…).
