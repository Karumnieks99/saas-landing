Amber five-star rating row used in testimonials and hero social proof.

```jsx
<Stars count={5} />
<Stars count={4} size={16} />
```

Filled stars use `--rating` (amber-400); empty stars fall back to zinc-200.
