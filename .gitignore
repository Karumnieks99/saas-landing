# Dependencies
node_modules/

# Production build
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.dev.log

# Editor/OS junk
.DS_Store
Thumbs.db

# Environment / local overrides
.env
.env.*
*.local

# Verification tooling + screenshots (local-only)
.cdp-shot.mjs
*.shot.png
screenshots/

# Windows reserved-name junk (e.g. from a botched `> nul` redirect)
nul

# Installed agent skills + Claude Code config (tooling, not site code)
.agents/
.claude/
skills-lock.json
